import React from "react";
import { View, Text } from "react-native";
import AttendeeItem from "./AttendeeItem";

const AttendeesList = () => {
    return (
        <View>
            <AttendeeItem first_name={} last_name={} checkedIn={}/>
        </View>
    );
};

export default AttendeesList;